/*
 * StringForm Messages
 *
 * This contains all the text for the StringForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.StringForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StringForm component!',
  },
});
